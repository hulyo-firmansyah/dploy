import { useRouter } from "next/router"
import { Fragment } from "react"

const Login = () => {
	/**
	 * Router
	 */
	const router = useRouter()

	/**
	 * Effects
	 */

	return <Fragment>Okez</Fragment>
}

export default Login
